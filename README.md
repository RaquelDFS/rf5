COMO BAIXAR O PROJETO:
Em uma pasta vazia você vai abrir o terminal (CERTIFIQUE SE DE USAR O BASH E NÃO O POWERSHELL)
e vai digitar o comando:
git clone
cd rf5
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
streamlit run app.py

    usuarios = [
        ("raquel",  "123",    "analista"),
        ("Wallace", "123456", "cliente"),
        ("Caio",    "123456", "cliente")
    ]

---

python -m venv venv
.\venv\Scripts\Activate.ps1
python -m streamlit run app.py
pip install pandas

---
